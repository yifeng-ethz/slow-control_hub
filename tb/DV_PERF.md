# Compatibility Redirect

The canonical performance/stress plan file is now [DV_PROF.md](DV_PROF.md).

This compatibility file exists only so older notes, scripts, and commit history
that still reference `DV_PERF.md` do not break immediately during the migration
to the exact `dv-workflow` naming.
